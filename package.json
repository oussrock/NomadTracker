{
  "name": "nomad-tracker-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm install",
    "build": "npm run install:all && cd client && npm install && npm run build",
    "start": "node server/index.js",
    "dev": "echo 'Run client and server separately for development' && echo 'npm run dev --prefix client' && echo 'npm run dev --prefix server'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
