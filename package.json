{
  "name": "nomad-tracker-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build:client": "npm run build --prefix client",
    "build:server": "cd server && npm install",
    "build": "npm run build:client",
    "start:prod": "cd server && node index.js",
    "dev": "echo 'Run client and server separately for development' && echo 'npm run dev --prefix client' && echo 'npm run dev --prefix server'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
